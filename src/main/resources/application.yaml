spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/neuropsy
    username: postgres
    password: ganc
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
